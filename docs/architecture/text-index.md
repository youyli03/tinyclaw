# Text index: where each feature's copy lives

<!-- GENERATED by scripts/text-index.ts — do not edit by hand.
     Add or fix a topic in that file's TOPICS table, then run:
       npm run text-index -- --write -->

Changing a behaviour means changing **all** of its text, not just the implementation:
implementation comments, tool specs (what the model reads), the built-in system prompt,
schema/config-example field docs, `docs/**`, the model-facing manuals in `docs/manual/**`,
`README.md`, `AGENTS.md`, CLI help, and the tests that assert the old wording.

## How to use it

```bash
npm run text-index -- --list        # every topic id
npm run text-index -- wake          # locate all copy for one topic (file:line, grouped by layer)
npm run text-index -- cron loop job # several topics at once
npm run text-index -- --all         # everything
```

The output ends with a **coverage line**: layers this topic declares that produced *zero* hits.
That is usually the copy you forgot — go write it. `grep` alone is not enough here because copy
often describes a behaviour without naming it (e.g. wake permissions written as "unattended" /
"whitelist"), so each topic carries the *specific* literal strings that mark its text.

## Layers

| layer | what lives there |
|---|---|
| `manual` | Model-facing manuals (`docs/manual/**`, ephemeral, loaded via the `manual` tool) |
| `cli` | CLI help/usage strings (`src/cli/**`, `bin/**`) |
| `tool` | Tool specs: name/description/parameter docs (`src/tools/**`) |
| `prompt` | Built-in system prompts (`src/code/system-prompt.ts`, `src/instructions/**`; the chat prompt template lives in `src/core/agent.ts`) |
| `tests` | Tests and probe expectations (`tests/**`) |
| `config-example` | Example config with field docs (`*.example.toml`) |
| `readme` | `README.md` |
| `agents` | `AGENTS.md` (facts + trap list) |
| `docs` | Human docs (`docs/**`, excluding `docs/manual`) |
| `code` | Implementation and its comments (`src/**`) |
| `other` | Anything else tracked (skills, mcp-servers, root scripts) |

## Topics

### `cron` — Cron jobs (scheduled tasks)

- **Canonical source**: `src/cron/schema.ts`, `src/cron/runner.ts`, `src/cron/scheduler.ts`, `src/tools/cron.ts`
- **Copy must exist in**: `code`, `tool`, `config-example`, `docs`, `manual`, `readme`, `agents`, `cli`
- **Grep terms**: `cron_add`, `cron_run`, `cron job`, `cron/<id>`, `TINYCLAW_CRON_JOB_ID`, `intervalSecs`, `timesOfDay`, `timeRange`, `runAt`, `stateful`, `mfaExempt`, `clearSessionOnRun`, `logLevel`, `PipelineStep`

### `loop` — Loop triggers (watch mode) and loop sessions

- **Canonical source**: `src/core/loop-trigger.ts`, `src/core/loop-runner.ts`, `src/tools/loop-control.ts`, `src/tools/loop-exit.ts`
- **Copy must exist in**: `code`, `tool`, `docs`, `manual`, `readme`, `agents`
- **Grep terms**: `loop_control`, `loop_exit`, `loop-trigger`, `loop session`, `tickSeconds`, `preCheckScript`, `bindTo`, `allowExit`, `timeRanges`, `loops/<id>.json`, `LoopRunner`

### `job` — Background process jobs (job_start and friends)

- **Canonical source**: `src/core/job-manager.ts`, `src/core/systemd-run.ts`, `src/tools/jobs.ts`
- **Copy must exist in**: `code`, `tool`, `docs`, `manual`, `readme`, `agents`, `tests`
- **Grep terms**: `job_start`, `job_output`, `job_kill`, `job_status`, `job_list`, `TINYCLAW_JOB_ID`, `.tinyclaw/jobs`, `systemd-run`, `detach`, `timeout_secs`, `MAX_RUNNING_TOTAL`

### `env` — Environment variables for agents / jobs / cron

- **Canonical source**: `src/config/agent-env.ts`, `src/cron/run-env.ts`, `src/tools/env-admin.ts`, `src/core/systemd-run.ts`
- **Copy must exist in**: `code`, `tool`, `config-example`, `docs`, `manual`, `readme`, `agents`
- **Grep terms**: `agents/<id>/env`, `.tinyclaw/env`, `env_set`, `env_list`, `env_delete`, `buildJobEnvBase`, `buildCronRunEnv`, `withWakeShimPath`, `extraEnv`, `loadDotEnv`

### `wake` — Waking a session (wake tool / CLI / shim / IPC)

- **Canonical source**: `src/main.ts`, `src/tools/wake.ts`, `src/core/wake-shim.ts`, `src/ipc/protocol.ts`, `src/core/session-channel.ts`
- **Copy must exist in**: `code`, `tool`, `docs`, `manual`, `readme`, `agents`, `cli`, `config-example`
- **Grep terms**: `wake(`, `wake `, `TINYCLAW_WAKE_TARGET`, `WAKE_MIN_INTERVAL_MS`, `wake-shim`, `agent.sock`, `woken`, `mfaForSession`, `notifyForSession`, `唤醒`, `origin=cron`

### `manual` — On-demand manuals and ephemeral tool results

- **Canonical source**: `src/tools/manual.ts`, `docs/manual`, `src/core/session.ts`, `src/tools/registry.ts`
- **Copy must exist in**: `code`, `tool`, `docs`, `readme`, `agents`
- **Grep terms**: `docs/manual`, `MANUAL_VERSION`, `ephemeralResult`, `_ephemeralCallIds`, `_serializeForPersist`, `dropEphemeralMessages`, `isEphemeralResultTool`

### `origin-permissions` — Run origin, unattended whitelist and MFA gating

- **Canonical source**: `src/auth/tool-policy.ts`, `src/security/audit.ts`, `src/config/schema.ts`, `src/core/agent.ts`
- **Copy must exist in**: `code`, `tool`, `config-example`, `docs`, `manual`, `readme`, `agents`
- **Grep terms**: `unattended`, `allowedTools`, `HARD_DENY_REACT_UNATTENDED`, `mfaFallback`, `isUnattended`, `RunOrigin`, `approvalPolicy`, `mfaApprovedForThisRun`, `requiresMFA`, `无人值守`, `白名单`

### `sandbox` — Sandbox (bwrap), path guard, elevation and fs_grant

- **Canonical source**: `src/sandbox/bwrap.ts`, `src/sandbox/elevation.ts`, `src/auth/fs-grant.ts`, `src/tools/path-guard.ts`
- **Copy must exist in**: `code`, `tool`, `config-example`, `docs`, `manual`, `readme`, `agents`
- **Grep terms**: `[sandbox]`, `bwrap`, `writablePaths`, `extraRwPaths`, `elevate`, `fs_grant`, `maskSecrets`, `readableSecretPaths`, `checkWritePath`, `selfAccess`

### `secrets` — Secret access control and ${SECRET:NAME} references

- **Canonical source**: `src/auth/secrets-access.ts`, `src/mcp/secret-ref.ts`, `src/config/secret-placeholders.ts`, `src/config/schema.ts`
- **Copy must exist in**: `code`, `tool`, `config-example`, `docs`, `readme`, `agents`, `tests`
- **Grep terms**: `secrets.toml`, `${SECRET:`, `[secrets]`, `canReadSecrets`, `resolveSecretRefs`, `secretRefName`, `secrets-access`

### `memory-mem` — MEM.md / ACTIVE.md distillation and injection budget

- **Canonical source**: `src/memory/mem-budget.ts`, `src/core/memory-maintenance.ts`, `src/core/agent.ts`
- **Copy must exist in**: `code`, `tool`, `config-example`, `docs`, `readme`, `agents`
- **Grep terms**: `MEM.md`, `MEM_SECTION_KEYS`, `memInjectionMaxChars`, `memory_write_mem`, `memory_read_mem`, `DISTILL_MEM_SYSTEM`, `ACTIVE.md`, `renderMemForPrompt`

### `memory-journal` — Append-only journal, memory_recall / memory_expand

- **Canonical source**: `src/memory/journal.ts`, `src/tools/recall.ts`, `src/core/session.ts`
- **Copy must exist in**: `code`, `tool`, `config-example`, `docs`, `readme`, `agents`
- **Grep terms**: `journal`, `memory_recall`, `memory_expand`, `journalEnabled`, `JOURNAL_SUFFIX`, `readJournal`

### `compaction` — Context compaction, pruning and transcript

- **Canonical source**: `src/memory/summarizer.ts`, `src/core/session.ts`, `src/memory/tool-result-pruner.ts`, `src/memory/transcript.ts`
- **Copy must exist in**: `code`, `docs`, `readme`, `agents`
- **Grep terms**: `summarizeAndCompress`, `compressForCode`, `CHAT_RETAIN_RATIO`, `rewriteJsonl`, `sanitizeMessages`, `pruneToolResults`, `transcript`, `maybeCompress`

### `tokens` — Token accounting, cache hit rate and Dashboard metrics

- **Canonical source**: `src/memory/token-estimate.ts`, `src/core/agent.ts`, `src/web/backend/collector.ts`
- **Copy must exist in**: `code`, `docs`, `readme`
- **Grep terms**: `classifyTokenSource`, `insertTokenUsageOnly`, `cacheHitRate`, `cacheReadTokens`, `promptTokens`, `latestBreakdown`, `缓存命中`

### `mcp` — MCP servers, their config file and hot reload

- **Canonical source**: `src/mcp/config-writer.ts`, `src/mcp/client.ts`, `src/tools/mcp-admin.ts`, `mcp.example.toml`
- **Copy must exist in**: `code`, `tool`, `config-example`, `docs`, `readme`, `agents`, `tests`, `cli`
- **Grep terms**: `mcp.toml`, `mcp_server_add`, `mcp_reload`, `mcp_enable_server`, `mcp-servers`

### `config-reload` — Config validation, hot reload, rollback and SAFE MODE

- **Canonical source**: `src/config/reload.ts`, `src/config/reload-plan.ts`, `src/config/state.ts`, `src/config/safe-mode.ts`, `src/config/validate.ts`
- **Copy must exist in**: `code`, `tool`, `config-example`, `docs`, `readme`, `agents`, `tests`, `cli`
- **Grep terms**: `config_reload`, `config_set`, `SAFE MODE`, `safe_config`, `last-known-good`, `settable-paths`, `invalidateConfigCache`, `config.toml.lkg`

### `qqbot` — QQBot connector (media, streaming, MFA prompts)

- **Canonical source**: `src/connectors/qqbot/index.ts`, `src/connectors/qqbot/outbound.ts`
- **Copy must exist in**: `code`, `config-example`, `docs`, `readme`, `agents`
- **Grep terms**: `QQBot`, `[channels.qqbots]`, `stream_messages`, `remain_msg_len`, `STREAM_MAX_BYTES`, `buildMFARequest`, `upload_prepare`, `file_data`, `clearTokenCache`

### `subagent` — Sub-agents: agent_fork / slave sessions / delegation

- **Canonical source**: `src/core/slave-manager.ts`, `src/tools/agent-fork.ts`, `src/tools/agent-binding.ts`
- **Copy must exist in**: `code`, `tool`, `config-example`, `docs`, `readme`, `agents`
- **Grep terms**: `agent_fork`, `agent_wait`, `agent_abort`, `slave:`, `autoForkThresholdMs`, `slaveManager`, `isSlave`

### `code-mode` — Code mode, plan mode and project binding

- **Canonical source**: `src/code/system-prompt.ts`, `src/code/commands.ts`, `src/core/project-router.ts`
- **Copy must exist in**: `code`, `tool`, `prompt`, `docs`, `readme`, `agents`
- **Grep terms**: `exit_plan_mode`, `/code`, `plan mode`, `codedir`, `projectSlug`, `code.jsonl`, `autoFork`

### `skills` — Skills: SKILLS.md, skill_run, create_skill

- **Canonical source**: `src/skills/registry.ts`, `src/skills/watcher.ts`, `src/tools/skill-run.ts`
- **Copy must exist in**: `code`, `tool`, `docs`, `readme`
- **Grep terms**: `SKILLS.md`, `skill_run`, `create_skill`, `skillRegistry`, `skills/`

### `web` — Web Dashboard, downloads and reports

- **Canonical source**: `src/web/backend/server.ts`, `src/web/backend/downloads.ts`, `src/tools/release-file.ts`
- **Copy must exist in**: `code`, `tool`, `config-example`, `docs`, `readme`
- **Grep terms**: `dashboard`, `release_file`, `write_report`, `db_write`, `/__login`, `downloads`, `web.token`

### `instructions` — Workspace instruction injection (AGENTS.md / CLAUDE.md)

- **Canonical source**: `src/instructions/workspace-prompt.ts`, `src/instructions/agents-md.ts`
- **Copy must exist in**: `code`, `prompt`, `docs`, `agents`
- **Grep terms**: `workspace-prompt`, `baselineIdentity`, `AGENTS.md`, `CLAUDE.md`, `instructions/`

## Adding a topic

Append an entry to `TOPICS` in `scripts/text-index.ts`:

```ts
{
  id: "my-feature",
  title: "What it is",
  canonical: ["src/path/that/defines/it.ts"],
  terms: ["specific_literal", "ConfigKeyName", "ENV_VAR_NAME"],
  layers: ["code", "tool", "docs", "manual", "readme", "agents"],
}
```

Then run `npm run text-index -- --write` to regenerate this file. Keep `terms` **specific**
(a bare `env` or `config` matches thousands of lines and defeats the purpose).
